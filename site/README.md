# ClawGuard Site

This directory contains the lightweight Astro marketing site for `clawguard.top`.

## Local development

```bash
pnpm --dir site install
pnpm --dir site dev
```

## Build

```bash
pnpm --dir site build
```

The site is deployed through GitHub Pages via `.github/workflows/deploy-site.yml`.
