---
import BaseLayout from '../../layouts/BaseLayout.astro';

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'Article',
  headline: '如何阻止 OpenClaw 泄露 API Key 和敏感信息',
  description: 'ClawGuard 通过 secret leak prevention，在 API key、Token、私钥、内部文档和敏感配置外发前先拦截。',
  url: 'https://clawguard.top/scenarios/secret-leak-prevention/',
  author: {
    '@type': 'Organization',
    name: 'ClawGuard'
  }
};
---

<BaseLayout
  title="如何阻止 OpenClaw 泄露 API Key 和敏感信息 | ClawGuard"
  description="ClawGuard 通过 secret leak prevention，在 API key、Token、私钥、内部文档和敏感配置外发前先拦截。"
  keywords={['OpenClaw 泄密', 'secret leak prevention', 'API key leak', 'OpenClaw secret scanning', '敏感信息外发拦截']}
  type="article"
  structuredData={structuredData}
>
  <main class="page-shell updates-shell">
    <section class="container updates-hero">
      <a class="back-link" href="/">← 返回首页</a>
      <span class="section-tag">Scenario</span>
      <h1>如何阻止 OpenClaw 泄露 API Key 和敏感信息</h1>
      <p>
        对普通用户和轻团队来说，最直观的恐惧往往不是“模型越权”这四个字，而是“它会不会把 key、文档、聊天记录发出去”。ClawGuard 的 secret leak prevention 就是为这个问题而生的。
      </p>
    </section>

    <section class="container update-list single-column">
      <article class="update-card">
        <h2>为什么 secret leak prevention 很关键</h2>
        <p>API key、Token、私钥、敏感配置、内部文档和聊天记录，一旦外发往往不是“麻烦一下”，而是直接变成账户风险、费用风险、隐私风险和供应链风险。</p>
      </article>
      <article class="update-card">
        <h2>ClawGuard 怎么识别</h2>
        <p>ClawGuard 会在 outbound 动作和 workspace mutation 动作里检查高置信度 secret 模式、敏感配置字段、私钥片段以及明显异常的外传信号。命中后要么直接阻断，要么进入审批。</p>
      </article>
      <article class="update-card">
        <h2>用户最终得到什么</h2>
        <p>不是事后才知道“原来东西已经发出去了”，而是在外发前就被挡住，并且知道为什么被挡住。对小白用户来说，这种清晰、直观、即时的保护比抽象的“安全能力”更重要。</p>
      </article>
    </section>
  </main>
</BaseLayout>
